[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "ai-memory-hub-native"
version = "0.1.0"
description = "Native Rust extensions for ai-memory-hub"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
